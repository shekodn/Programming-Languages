;repetidos
#lang racket
;repetidos

(define l (list 1 2 3 4 5))

(define (repetidos? list)
  (cond ((null? list) #f)
        ((member (car list) (cdr list)) #t)
        (else (repetidos? (cdr list)))))

(repetidos? 1 2 3 4 5)
(repetidos? 1 -2 1 -2 1)
